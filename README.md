# Smart-Stick
In this project we configured a FPGA to vibrate when an object is near it using a vibrating motor and an infrared sensor , it also turn on an alarm if the owner lost contact with the stick using a buzzer and a touch sensor. The project was implemented using VHDL.
